# Disposable repro workflow. Delete after validation.

name: "PR 10162 edit-reply tool-order A/B"

on:
  push:
    branches:
      - "mimir-pr10162-pos-20260901221019"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  repro:
    name: "Targeted repro"
    runs-on: "ubuntu-latest"
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
        with:
          persist-credentials: false

      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e
        with:
          node-version: "22"

      - name: "Node version"
        shell: bash
        run: |
          node --version

      - name: "Install frontend deps"
        shell: bash
        run: |
          cd studio/frontend && npm ci --no-audit --no-fund

      - name: "PROBE: edit-reply tool-order round trip"
        shell: bash
        run: |
          cd studio/frontend && node --experimental-strip-types --test tests/edit-reply-keeps-tool-order.test.ts

